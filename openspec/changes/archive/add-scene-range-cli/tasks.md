# タスク: シーン範囲指定による部分動画出力

## 1. CLI引数の追加

- [x] 1.1 `--scenes` オプションを `generate` コマンドに追加
- [x] 1.2 シーン範囲のパース関数を実装（`1-3` → (1, 3)、`2` → (2, 2)）
- [x] 1.3 範囲バリデーション（存在するセクション数を超えないか確認）

## 2. フレーズフィルタリング

- [x] 2.1 `section_index` を使って指定範囲のフレーズを抽出
- [x] 2.2 対応する `audio_paths` と `slide_paths` もフィルタリング

## 3. 出力ファイル名の調整

- [x] 3.1 `--scenes` 指定時は出力ファイル名に範囲を含める
- [x] 3.2 例: `output_scenes_1-3.mp4`

## 4. Remotionレンダリング対応

- [x] 4.1 フィルタリングされたフレーズでcomposition.jsonを生成
- [x] 4.2 スライド参照のインデックス調整

## 5. テスト

- [x] 5.1 シーン範囲パースのユニットテスト
- [x] 5.2 範囲外指定時のエラーハンドリングテスト
- [x] 5.3 E2Eテスト（オプション指定時の動画生成確認）
