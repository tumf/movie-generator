# タスク: 動画の最後が切れる問題の修正

## 1. 実装

- [ ] 1.1 `getScenesWithTiming`関数を修正し、トランジションオーバーラップを考慮した`startFrame`を計算する
  - スライドグループ情報を受け取り、各シーンがどのグループに属するかを判定
  - グループが変わるたびに累積オーバーラップを加算
  - シーンの`startFrame`から累積オーバーラップを減算

- [ ] 1.2 `VideoGenerator`コンポーネントでトランジション情報を`getScenesWithTiming`に渡す

- [ ] 1.3 動作確認テストを実施
  - 複数スライドを持つ動画でトランジションあり/なしの両方をテスト
  - 最後のフレーズが完全に再生されることを確認

## 2. 検証

- [ ] 2.1 既存のテストが通ることを確認
- [ ] 2.2 トランジションなし（`none`または`duration_frames: 0`）の場合の互換性確認
