## 1. Implementation
- [ ] 1.1 設定の数値デフォルト/制約を命名定数へ集約する（検証: 主要な数値リテラルが定数参照に置換されることを確認）

## 2. Tests
- [ ] 2.1 既存の設定ロード/検証テストが維持される（検証: `uv run pytest -k config -v`）

## 3. Verification
- [ ] 3.1 全テストが通る（検証: `uv run pytest`）
