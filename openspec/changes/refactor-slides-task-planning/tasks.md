## 1. Implementation
- [ ] 1.1 スライド生成のタスク計画（対象決定/スキップ/出力パス）を純粋処理として抽出する（検証: 実行処理と分離されていることを確認）
- [ ] 1.2 実行側は計画済みタスクの実行に専念する（検証: 並列度/リトライの責務が実行側に集約されていることを確認）

## 2. Tests
- [ ] 2.1 既存スライドがある場合のスキップ計画をユニットテストで検証する（検証: `uv run pytest -k slides -v`）

## 3. Verification
- [ ] 3.1 全テストが通る（検証: `uv run pytest`）
