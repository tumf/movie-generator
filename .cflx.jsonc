{
  // Conflux Configuration - Project-specific overrides

  // Lifecycle hooks
  "hooks": {
    // Rebuild indexes after merge completion
    "on_merged": "cargo bump patch -g && tldr warm . --lang rust && tldr semantic index . --lang rust && leann build openspec-spec --docs ./src"
  }
}
